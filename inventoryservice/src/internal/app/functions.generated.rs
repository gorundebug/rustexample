// Code generated by servicegen. DO NOT EDIT.

#![allow(dead_code, unused_imports)]

use super::imports::*;
use super::makers_generated::ServiceMakers;

pub struct ServiceFunctions {
    pub get_inventory_item_data: Arc<GetInventoryItemData>,
    pub get_inventory_item_error: Arc<GetInventoryItemError>,
    pub process_order_item_source: ProcessOrderItemSource,
}
struct FunctionGroup0Results {
    get_inventory_item_data: Option<Arc<GetInventoryItemData>>,
    get_inventory_item_error: Option<Arc<GetInventoryItemError>>,
    process_order_item_source: Option<ProcessOrderItemSource>,
}

fn init_function_group_0(
    maker_group_context: MessageContext,
    environment: RuntimeEnvironment,
    makers: &ServiceMakers,
    maker_error_sender: mpsc::Sender<RuntimeError>,
) -> Pin<Box<dyn Future<Output = FunctionGroup0Results> + Send>> {
    let get_inventory_item_data_maker = makers.get_inventory_item_data.clone();
    let get_inventory_item_error_maker = makers.get_inventory_item_error.clone();
    let process_order_item_source_maker = makers.process_order_item_source.clone();
    Box::pin(async move {
        let get_inventory_item_data_context = maker_group_context.clone();
        let get_inventory_item_data_group_context = maker_group_context.clone();
        let get_inventory_item_data_environment = environment.clone();
        let get_inventory_item_data_error_sender = maker_error_sender.clone();
        let get_inventory_item_data_future = async move {
            let result = (get_inventory_item_data_maker)(
                get_inventory_item_data_context,
                get_inventory_item_data_environment,
            ).await;
            match result {
                Ok(value) => Some(Arc::new(value)),
                Err(error) => {
                    get_inventory_item_data_group_context.cancel();
                    get_inventory_item_data_error_sender.send(error)
                        .expect("function maker error receiver was dropped");
                    None
                }
            }
        };
        let mut get_inventory_item_data = None;
        let get_inventory_item_error_context = maker_group_context.clone();
        let get_inventory_item_error_group_context = maker_group_context.clone();
        let get_inventory_item_error_environment = environment.clone();
        let get_inventory_item_error_error_sender = maker_error_sender.clone();
        let get_inventory_item_error_future = async move {
            let result = (get_inventory_item_error_maker)(
                get_inventory_item_error_context,
                get_inventory_item_error_environment,
            ).await;
            match result {
                Ok(value) => Some(Arc::new(value)),
                Err(error) => {
                    get_inventory_item_error_group_context.cancel();
                    get_inventory_item_error_error_sender.send(error)
                        .expect("function maker error receiver was dropped");
                    None
                }
            }
        };
        let mut get_inventory_item_error = None;
        let process_order_item_source_context = maker_group_context.clone();
        let process_order_item_source_group_context = maker_group_context.clone();
        let process_order_item_source_environment = environment.clone();
        let process_order_item_source_error_sender = maker_error_sender.clone();
        let process_order_item_source_future = async move {
            let result = (process_order_item_source_maker)(
                process_order_item_source_context,
                process_order_item_source_environment,
            ).await;
            match result {
                Ok(value) => Some(value),
                Err(error) => {
                    process_order_item_source_group_context.cancel();
                    process_order_item_source_error_sender.send(error)
                        .expect("function maker error receiver was dropped");
                    None
                }
            }
        };
        let mut process_order_item_source = None;
        let maker_futures: Vec<Pin<Box<dyn Future<Output = ()> + Send + '_>>> = vec![
            Box::pin(async { get_inventory_item_data = get_inventory_item_data_future.await; }),
            Box::pin(async { get_inventory_item_error = get_inventory_item_error_future.await; }),
            Box::pin(async { process_order_item_source = process_order_item_source_future.await; }),
        ];
        futures_util::future::join_all(maker_futures).await;
        FunctionGroup0Results {
            get_inventory_item_data,
            get_inventory_item_error,
            process_order_item_source,
        }
    })
}

impl ServiceFunctions {
    pub async fn init_functions(
        context: MessageContext,
        environment: RuntimeEnvironment,
        makers: &ServiceMakers,
    ) -> RuntimeResult<Self> {
        let _ = (&context, &environment, makers);
        let maker_group_context = context.child();
        let (maker_error_sender, maker_error_receiver) = mpsc::channel::<RuntimeError>();
        let group_0 = init_function_group_0(
            maker_group_context.clone(), environment.clone(), makers, maker_error_sender.clone(),
        ).await;
        maker_group_context.cancel();
        drop(maker_error_sender);
        if let Ok(error) = maker_error_receiver.try_recv() {
            return Err(error);
        }
        Ok(Self {
            get_inventory_item_data: group_0.get_inventory_item_data.ok_or_else(|| RuntimeError::InvalidConfiguration(
                "function maker get_inventory_item_data failed without an error".to_owned(),
            ))?,
            get_inventory_item_error: group_0.get_inventory_item_error.ok_or_else(|| RuntimeError::InvalidConfiguration(
                "function maker get_inventory_item_error failed without an error".to_owned(),
            ))?,
            process_order_item_source: group_0.process_order_item_source.ok_or_else(|| RuntimeError::InvalidConfiguration(
                "function maker process_order_item_source failed without an error".to_owned(),
            ))?,
        })
    }
}

pub struct ServiceHandlers {
    pub process_order_item_source: ProcessOrderItemSource,
}


impl ServiceHandlers {
    pub fn reload(&self, config: &Config) {
        let _ = config;
    }
}