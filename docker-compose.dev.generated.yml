# Code generated by servicegen. DO NOT EDIT.
# This overlay is the only project-level Compose configuration that mounts
# service source code. Use it through `make docker-up-dev`.
services:
  analyticsservice:
    image: rustexample-rust-development:latest
    build:
      context: .
      dockerfile: Dockerfile
      target: development
      extra_hosts:
        - "host.docker.internal:host-gateway"
      args:
        SERVICEGEN_GITHUB_RAW_URL: ${SERVICEGEN_GITHUB_RAW_URL:-https://github.com}
        CARGO_REGISTRIES_CRATES_IO_INDEX: ${CARGO_REGISTRIES_CRATES_IO_INDEX:-https://github.com/rust-lang/crates.io-index}
        SERVICEGEN_MAVEN_CENTRAL_URL: ${SERVICEGEN_MAVEN_CENTRAL_URL:-https://repo1.maven.org/maven2}
        SERVICEGEN_APT_DEBIAN_URL: ${SERVICEGEN_APT_DEBIAN_URL:-}
        SERVICEGEN_APT_DEBIAN_SECURITY_URL: ${SERVICEGEN_APT_DEBIAN_SECURITY_URL:-}
      additional_contexts:
        rustservicelib-source: "${RUSTSERVICELIB_SOURCE_CONTEXT:-https://github.com/gorundebug/rustservicelib.git#v0.2.24}"
    working_dir: /workspace/rustexample
    command:
      - /bin/sh
      - -lc
      - >-
        exec cargo run --locked -p analyticsservice --
        --config /workspace/rustexample/analyticsservice/config/config.yaml
        --values /workspace/rustexample/analyticsservice/config/overrides.yaml
    environment:
      CARGO_REGISTRIES_CRATES_IO_INDEX: ${CARGO_REGISTRIES_CRATES_IO_INDEX:-https://github.com/rust-lang/crates.io-index}
      GIT_CONFIG_COUNT: ${GIT_CONFIG_COUNT:-0}
      GIT_CONFIG_KEY_0: ${GIT_CONFIG_KEY_0:-}
      GIT_CONFIG_VALUE_0: ${GIT_CONFIG_VALUE_0:-}
      GIT_CONFIG_KEY_1: ${GIT_CONFIG_KEY_1:-}
      GIT_CONFIG_VALUE_1: ${GIT_CONFIG_VALUE_1:-}
    volumes:
      - .:/workspace/rustexample
      - rust-dev-cargo-registry:/usr/local/cargo/registry
      - rust-dev-cargo-git:/usr/local/cargo/git
      - rust-dev-target:/workspace/rustexample/target
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
  automationservice:
    image: automationservice-development:latest
    build:
      context: .
      dockerfile: automationservice/Dockerfile
      target: development
      extra_hosts:
        - "host.docker.internal:host-gateway"
      args:
        SERVICE_DIR: automationservice
        GOPROXY: ${GOPROXY:-direct}
        GOSUMDB: ${GOSUMDB:-sum.golang.org}
        SERVICEGEN_GITHUB_RAW_URL: ${SERVICEGEN_GITHUB_RAW_URL:-https://github.com}
      additional_contexts:
        servicelib-source: "${GOSERVICELIB_SOURCE_CONTEXT:-https://github.com/gorundebug/servicelib.git#v0.2.24}"
    working_dir: /workspace/automationservice
    command:
      - go
      - run
      - ./cmd/service/main.go
      - -config
      - ./config/config.yaml
      - -values
      - ./config/overrides.yaml
    environment:
      GOPROXY: ${GOPROXY:-direct}
      GOSUMDB: ${GOSUMDB:-sum.golang.org}
      GIT_CONFIG_COUNT: ${GIT_CONFIG_COUNT:-0}
      GIT_CONFIG_KEY_0: ${GIT_CONFIG_KEY_0:-}
      GIT_CONFIG_VALUE_0: ${GIT_CONFIG_VALUE_0:-}
      GIT_CONFIG_KEY_1: ${GIT_CONFIG_KEY_1:-}
      GIT_CONFIG_VALUE_1: ${GIT_CONFIG_VALUE_1:-}
    volumes:
      - .:/workspace
      - go-dev-mod-cache:/go/pkg/mod
      - go-dev-build-cache:/root/.cache/go-build
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
  inventoryservice:
    image: rustexample-rust-development:latest
    build:
      context: .
      dockerfile: Dockerfile
      target: development
      extra_hosts:
        - "host.docker.internal:host-gateway"
      args:
        SERVICEGEN_GITHUB_RAW_URL: ${SERVICEGEN_GITHUB_RAW_URL:-https://github.com}
        CARGO_REGISTRIES_CRATES_IO_INDEX: ${CARGO_REGISTRIES_CRATES_IO_INDEX:-https://github.com/rust-lang/crates.io-index}
        SERVICEGEN_MAVEN_CENTRAL_URL: ${SERVICEGEN_MAVEN_CENTRAL_URL:-https://repo1.maven.org/maven2}
        SERVICEGEN_APT_DEBIAN_URL: ${SERVICEGEN_APT_DEBIAN_URL:-}
        SERVICEGEN_APT_DEBIAN_SECURITY_URL: ${SERVICEGEN_APT_DEBIAN_SECURITY_URL:-}
      additional_contexts:
        rustservicelib-source: "${RUSTSERVICELIB_SOURCE_CONTEXT:-https://github.com/gorundebug/rustservicelib.git#v0.2.24}"
    working_dir: /workspace/rustexample
    command:
      - /bin/sh
      - -lc
      - >-
        exec cargo run --locked -p inventoryservice --
        --config /workspace/rustexample/inventoryservice/config/config.yaml
        --values /workspace/rustexample/inventoryservice/config/overrides.yaml
    environment:
      CARGO_REGISTRIES_CRATES_IO_INDEX: ${CARGO_REGISTRIES_CRATES_IO_INDEX:-https://github.com/rust-lang/crates.io-index}
      GIT_CONFIG_COUNT: ${GIT_CONFIG_COUNT:-0}
      GIT_CONFIG_KEY_0: ${GIT_CONFIG_KEY_0:-}
      GIT_CONFIG_VALUE_0: ${GIT_CONFIG_VALUE_0:-}
      GIT_CONFIG_KEY_1: ${GIT_CONFIG_KEY_1:-}
      GIT_CONFIG_VALUE_1: ${GIT_CONFIG_VALUE_1:-}
    volumes:
      - .:/workspace/rustexample
      - rust-dev-cargo-registry:/usr/local/cargo/registry
      - rust-dev-cargo-git:/usr/local/cargo/git
      - rust-dev-target:/workspace/rustexample/target
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
  orderservice:
    image: rustexample-rust-development:latest
    build:
      context: .
      dockerfile: Dockerfile
      target: development
      extra_hosts:
        - "host.docker.internal:host-gateway"
      args:
        SERVICEGEN_GITHUB_RAW_URL: ${SERVICEGEN_GITHUB_RAW_URL:-https://github.com}
        CARGO_REGISTRIES_CRATES_IO_INDEX: ${CARGO_REGISTRIES_CRATES_IO_INDEX:-https://github.com/rust-lang/crates.io-index}
        SERVICEGEN_MAVEN_CENTRAL_URL: ${SERVICEGEN_MAVEN_CENTRAL_URL:-https://repo1.maven.org/maven2}
        SERVICEGEN_APT_DEBIAN_URL: ${SERVICEGEN_APT_DEBIAN_URL:-}
        SERVICEGEN_APT_DEBIAN_SECURITY_URL: ${SERVICEGEN_APT_DEBIAN_SECURITY_URL:-}
      additional_contexts:
        rustservicelib-source: "${RUSTSERVICELIB_SOURCE_CONTEXT:-https://github.com/gorundebug/rustservicelib.git#v0.2.24}"
    working_dir: /workspace/rustexample
    command:
      - /bin/sh
      - -lc
      - >-
        exec cargo run --locked -p orderservice --
        --config /workspace/rustexample/orderservice/config/config.yaml
        --values /workspace/rustexample/orderservice/config/overrides.yaml
    environment:
      CARGO_REGISTRIES_CRATES_IO_INDEX: ${CARGO_REGISTRIES_CRATES_IO_INDEX:-https://github.com/rust-lang/crates.io-index}
      GIT_CONFIG_COUNT: ${GIT_CONFIG_COUNT:-0}
      GIT_CONFIG_KEY_0: ${GIT_CONFIG_KEY_0:-}
      GIT_CONFIG_VALUE_0: ${GIT_CONFIG_VALUE_0:-}
      GIT_CONFIG_KEY_1: ${GIT_CONFIG_KEY_1:-}
      GIT_CONFIG_VALUE_1: ${GIT_CONFIG_VALUE_1:-}
    volumes:
      - .:/workspace/rustexample
      - rust-dev-cargo-registry:/usr/local/cargo/registry
      - rust-dev-cargo-git:/usr/local/cargo/git
      - rust-dev-target:/workspace/rustexample/target
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE

volumes:
  go-dev-mod-cache:
  go-dev-build-cache:
  rust-dev-cargo-registry:
  rust-dev-cargo-git:
  rust-dev-target:
  cpp-dev-cmake-build: